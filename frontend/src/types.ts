export interface DoctorData {
    id: any
    firstName: string
    lastName: string
    birthDate: string
    medicalTeamId: any
    active: boolean
}

export interface TeamData {
    id: any
    name: string
}